# AGENTS.md

This repo should stay small and practical.

Goals:

- help AI coding-agent users keep project context healthy
- avoid over-engineered frameworks
- prefer readable stdlib Python
- keep checks explainable and safe

Before release, run:

```bash
python3 -m agent_context_health.cli .
```
